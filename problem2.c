/* Write a C program that declares a variable of type 'char[]'
    to store your name and then outputs a greeting that includes your name.
    Use the printf function
*/

#include <stdio.h>

int main() {
    char name[] = "Mel Liza";

    printf("Hello, %s!", name );
}